run the project :
run backend server :port - 8000 
                    cd ->../price-track-wine\backend\server
                    enter : python -m uvicorn server:app --reload

run react app : port - 3000
                cd ->../price-track-wine/interface 
                enter : npm start


TASKS:
1. change the database : 
    - add new table in the name wine_prices_history
    - change the name of table wine_history to wine_current_price and add culomn counter 
     